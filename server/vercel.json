{
    "builds": [
        {
            "src": "/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        },{
  "rewrites": [
    { "source": "/(.*)", "destination": "api/server.js" }
  ]
}

        
    ]
    
}

